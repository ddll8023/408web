<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.web408.mapper.UserMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.web408.pojo.entity.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="email" property="email"/>
        <result column="role" property="role"/>
        <result column="enabled" property="enabled"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 根据用户名查询 -->
    <select id="findByUsername" resultMap="BaseResultMap">
        SELECT id, username, password, email, role, enabled, create_time, update_time
        FROM user
        WHERE username = #{username}
    </select>

    <!-- 插入用户 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, password, email, role, enabled)
        VALUES (#{username}, #{password}, #{email}, #{role}, #{enabled})
    </insert>

    <!-- 根据ID查询 -->
    <select id="findById" resultMap="BaseResultMap">
        SELECT id, username, password, email, role, enabled, create_time, update_time
        FROM user
        WHERE id = #{id}
    </select>

    <!-- 更新角色 -->
    <update id="updateRole">
        UPDATE user
        SET role = #{role}
        WHERE id = #{id}
    </update>

    <!-- 更新启用状态 -->
    <update id="updateEnabled">
        UPDATE user
        SET enabled = #{enabled}
        WHERE id = #{id}
    </update>

</mapper>

