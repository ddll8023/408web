# 408Web 后端结构迁移文档

> 文档状态：P1、P2-A、P2-B、P3-A（reporting）、P3-B（media）和 P4 静态清理、只读回归、N+1 列表优化已完成；P5 尚未开始。写入/上传接口行为按当前授权暂不验证
>
> 最近更新：2026-09-11
>
> 适用范围：`backend-fastapi/`
>
> 本文档记录首轮迁移的当前执行状态。P2-A、P2-B、P3-A、P3-B 和 P4 静态清理、只读回归、N+1 列表优化已完成，P5 仍待评估；不进行数据库重建或公开接口调整。

## 1. 迁移结论

后端迁移目标为：

> **模块化单体（Modular Monolith）+ 业务域纵向组织 + 模块内轻量分层**

不采用微服务，也不为每个模块强行增加 Domain、Port、Adapter、Factory 等抽象层。

当前项目已经形成 `backend-fastapi/app/main.py` 作为运行入口，因此第一阶段保留 `app` 根包，只增加 `app/modules/` 并逐模块迁移。暂不将项目改为 `src/` 布局，避免同时改变启动脚本、导入路径和部署方式。

未来如果重新创建后端，再考虑使用：

```text
backend-fastapi/src/web408/
```

## 2. 当前基线与已确认事实

### 2.1 运行与公开契约

- Python 要求为 `>=3.12`。
- 依赖使用 FastAPI、SQLModel、aiosqlite、Pydantic 2、pydantic-settings、JWT 和 aiofiles。
- 启动入口为 `app.main:app`。
- 数据库为本地 SQLite，默认路径为 `backend-fastapi/data/web408.db`。
- 图片默认保存在 `backend-fastapi/uploads/images/`。
- API 前缀为 `/api`。
- Web API 当前统一使用 POST。
- 成功响应使用 `{code, message, data}`。
- 分页数据使用 `lists` 和 `pagination`。
- 当前数据库初始化使用 SQLModel `create_all`，没有迁移链。
- 迁移开始前发现当前数据库与 Git 版本存在未提交数据差异；当前 `mock_question` 为 246 条，Git 版本为 232 条。经确认保留当前数据库作为迁移基线，不覆盖、不回滚、不清理。

### 2.2 当前代码组织

当前后端是“模块化业务目录 + 共享基础设施”的结构；遗留数据库表不再由应用模型映射：

```text
app/
├── api/                         # 路由聚合、通用 HTTP 会话依赖、上传路由
├── core/                        # 配置、异常、日志和安全
├── database/                    # Engine、Session 和建表入口
├── middleware/                  # CORS 中间件
├── models/                      # 共享基础模型和枚举
├── integrations/                # 本地文件存储适配
├── modules/
│   ├── auth/                    # P1 已迁移
│   ├── catalog/                 # P1 已迁移，含跨模块只读边界
│   ├── question_content/        # P2-A 已迁移
│   ├── exam/                    # P2-A 已迁移
│   ├── mock/                    # P2-A 已迁移
│   ├── reporting/               # P3-A 已迁移
│   └── media/                   # P3-B 已迁移
└── schemas/                     # 当前仅保留 common
```

`auth`、`catalog` 已完成 P1 归位，`question_content`、`exam`、`mock` 已完成 P2-A 业务代码迁移，ORM 模型已在 P2-B 拆分到对应模块，统计导出和媒体能力已分别在 P3-A、P3-B 迁入 `reporting`、`media`；顶层旧业务目录已清理。

### 2.3 当前仍需处理的结构问题

1. reporting/media 的写入、上传和清理接口回归按当前授权暂不执行。
2. `ExamQuestion.category` 和 `MockQuestion.category` 仍以分类名称 JSON 字符串保存，查询依赖字符串匹配，统计需要在内存解析；列表响应的科目/作者 N+1 已通过批量只读查询消除。
3. 更广泛的统计性能、分类 JSON 规范化和路径配置优化仍不属于本轮范围。
4. `exam_query_service.py` 的 `get_categories_by_subject()` 曾缺少 `json` 导入；当前代码已修复，不再作为未解决问题追踪。

## 3. 迁移原则

### 3.1 公开行为不变

第一阶段只调整内部结构，不改变：

- API URL、HTTP 方法和响应字段；
- 前端当前使用的请求参数和字段命名；
- 数据库表名、字段名和现有约束；
- 图片持久化相对路径规则；
- JWT 登录和管理员权限行为。

顶层 `app/api/router.py` 继续作为路由组合入口，通过新的模块路由注册现有前缀，例如 `/auth`、`/subject`、`/exam`、`/mock` 和 `/upload`。

### 3.2 单向依赖

```text
Router
  ↓
Query Service / Command Service
  ↓
Repository / 其他模块公开 Service / Storage
  ↓
Database / 文件系统
```

必须遵守：

- Router 不直接查询数据库或提交事务。
- Schema 不查询数据库、不判断权限、不调用外部服务。
- Repository 不调用 Service，不提交或回滚事务。
- Command Service 或跨模块 Workflow 拥有事务边界。
- 模块之间不得直接引用对方的 Repository 或私有实现。
- 统计、媒体和题库之间的跨模块读取统一通过公开 Service 和只读 DTO。
- `ExamQuestion` 与 `MockQuestion` 保持独立实体，只共享题目内容规则、序列化和校验。
- `core/` 和 `database/` 不依赖业务模块。
- 不创建没有实际职责的空文件和空目录。
- 不创建万能 `common/utils.py`、通用 Repository 基类或过早的接口层。

### 3.3 事务与异常

- 一个业务用例对应一个事务边界。
- 写入 Service 使用显式事务，Repository 只执行持久化操作。
- 查询 Service 不提交事务。
- 业务异常由全局异常处理器统一映射，路由不重复编写相同的 `try/except`。
- 文件写入使用临时文件和原子替换；文件系统操作失败时清理临时文件。

## 4. 目标目录结构

```text
backend-fastapi/
└── app/
    ├── main.py                         # 应用组合根、生命周期、静态目录和中间件
    ├── api/
    │   ├── router.py                   # 聚合各业务模块路由
    │   └── dependencies.py             # 跨模块 HTTP 依赖
    ├── core/
    │   ├── config.py                   # 全局配置
    │   ├── exceptions.py               # 全局异常及响应映射
    │   ├── logging.py                  # 日志配置
    │   └── security.py                 # 密码和 JWT 基础能力
    ├── database/
    │   └── connection.py               # Engine、Session 和建表入口
    ├── integrations/
    │   └── local_file_storage.py        # 本地文件系统边界
    └── modules/
        ├── auth/
        │   ├── router.py
        │   ├── dependencies.py
        │   ├── schemas.py
        │   ├── models.py
        │   ├── repository.py
        │   └── service.py
        ├── catalog/
        │   ├── router.py
        │   ├── schemas.py
        │   ├── models.py
        │   ├── repository.py
        │   ├── service.py
        │   ├── category_tree.py
        │   └── category_code.py
        ├── question_content/
        │   ├── schemas.py
        │   ├── serialization.py
        │   └── validation.py
        ├── exam/
        │   ├── router.py
        │   ├── schemas.py
        │   ├── models.py
        │   ├── repository.py
        │   ├── query_service.py
        │   ├── command_service.py
        │   └── mapper.py
        ├── mock/
        │   ├── router.py
        │   ├── schemas.py
        │   ├── models.py
        │   ├── repository.py
        │   ├── query_service.py
        │   ├── command_service.py
        │   └── mapper.py
        ├── reporting/
        │   ├── router.py
        │   ├── schemas.py
        │   ├── service.py
        │   ├── exam_category_stats.py
        │   └── exporters.py
        └── media/
            ├── router.py
            ├── schemas.py
            ├── service.py
            └── reference_reader.py
```

不默认创建 `__init__.py`、`tasks/`、测试数据库或数据库迁移目录。只有实际出现对应职责时才增加文件。

### 4.1 P2-B 完成后的实际结构

本节记录当前代码，不等同于最终目标结构：

```text
backend-fastapi/app/
├── api/
│   ├── router.py
│   └── dependencies.py
├── integrations/
│   └── local_file_storage.py              # P3-B 已迁移
├── models/
│   ├── base.py
│   └── enums.py
├── modules/
│   ├── auth/models.py
│   ├── catalog/
│   │   ├── models.py
│   │   ├── question_scope_repository.py
│   │   └── read_service.py
│   ├── question_content/
│   ├── exam/models.py
│   ├── mock/models.py
│   ├── reporting/                        # P3-A 已迁移
│   └── media/                            # P3-B 已迁移
└── schemas/
    └── common.py
```

SQLModel 模型由应用组合根在启动阶段显式加载当前业务模块模型；不依赖路由或 Repository 的间接导入顺序。历史遗留表仍保留在 SQLite 中，但不再建立对应 ORM 映射。

## 5. 模块边界

| 模块 | 负责内容 | 不负责内容 |
|---|---|---|
| `auth` | 用户、注册、登录、JWT、当前用户、管理员权限 | 题目和目录业务规则 |
| `catalog` | 科目、真题分类、分类树、分类移动、分类归属校验 | 题干、选项和答案内容 |
| `question_content` | 题型结构、选项、Markdown 字段的序列化和纯数据校验 | 数据库查询、权限和事务 |
| `exam` | 真题查询、索引、年份统计、查重、增删改 | 分类目录定义、跨域导出编排 |
| `mock` | 模拟题查询、来源统计、查重、增删改 | 真题年份语义和真题分类统计 |
| `reporting` | 真题分类统计、统计汇总、Markdown/Excel 导出 | 题库数据写入 |
| `media` | 图片上传、文件存储、引用扫描、删除和清理 | 自动修改题目内容 |
| 前端收藏 | 浏览器本地分类收藏 | 后端不创建收藏模块 |

### 5.1 章节和知识点

已确认：以 `ExamCategory` 作为题库唯一正式分类体系。

- 真题和模拟题的分类、统计、筛选和校验均只依赖 `ExamCategory`；
- `Chapter` 现有接口和模型继续保留在 `catalog`，因为它仍有公开 API；
- 不删除章节相关表或模型，避免破坏现有章节 API；
- 如果未来需要知识点文章，再单独设计知识内容模块及其关联关系。

## 6. 当前文件迁移映射

| 原位置（历史文件） | 当前/目标位置 | 状态与说明 |
|---|---|---|
| `app/api/auth.py` | `app/modules/auth/router.py` | P1 已完成，旧文件已移除，认证路由保持不变 |
| `app/api/subject.py`、`chapter.py`、`exam_category.py` | `app/modules/catalog/` | P1 已完成，旧目录路由已归位 |
| `app/api/exam.py` | `app/modules/exam/router.py` | P2-A 已完成，旧文件已移除，保留 `/exam` 前缀和 POST 路径 |
| `app/api/mock.py` | `app/modules/mock/router.py` | P2-A 已完成，旧文件已移除，保留 `/mock` 前缀和 POST 路径 |
| `app/api/upload.py` | `app/modules/media/router.py` | P3-B 已完成，旧上传路由已移除，保持 `/upload` 前缀 |
| `app/services/exam_query_service.py`、`exam_command_service.py` | `app/modules/exam/` | P2-A 已完成，查询和命令保持独立用例 |
| `app/services/mock_*` | `app/modules/mock/` | P2-A 已完成，纯转发门面已删除 |
| `app/services/category_*`、`category_code.py` | `app/modules/catalog/` | P1 已完成，分类树和编码已归入目录模块 |
| `app/services/exam_category_stats.py`、`exam_export_service.py` | `app/modules/reporting/` | P3-A 已完成，旧 Service 已移除，统计/导出通过题库公开只读 Service 获取数据 |
| `app/services/question_mapping.py`、`question_validation.py` | `app/modules/question_content/` | P2-A 已完成，已拆为纯内容 Schema、序列化和校验 |
| `app/services/upload_service.py` | `app/modules/media/service.py` | P3-B 已完成，文件校验与媒体用例已归位 |
| `app/repositories/exam_repository.py`、`mock_repository.py` | `app/modules/exam/`、`app/modules/mock/` | P2-A 已完成，题库 Repository 已按模块归位 |
| `app/repositories/question_scope_repository.py` | `app/modules/catalog/` + `read_service.py` | P2-A 已完成，题库通过目录公开只读边界访问 |
| `app/repositories/image_reference_repository.py` | `app/modules/media/reference_reader.py` | P3-B 已完成，通过题库公开 Query Service 扫描引用 |
| `app/models/entities.py` | `app/modules/auth/models.py`、`catalog/models.py`、`exam/models.py`、`mock/models.py` | P2-B 已完成，业务模型已按模块拆分；遗留表模型随后移除，数据库表不做删除 |
| `app/schemas/exam.py`、`mock.py`、`question.py` | `app/modules/exam/`、`app/modules/mock/`、`app/modules/question_content/` | P2-A 已完成，旧 Schema 路径已移除 |
| `app/api/dependencies.py` 中的认证逻辑 | `app/modules/auth/dependencies.py` | P1 已完成，`api` 仅保留通用 HTTP 会话依赖 |

## 7. 统计、媒体与跨模块协作

### 7.1 统计与导出

统计和导出虽然当前部分路由位于 `/exam`，实现上应归属 `reporting`：

- `exam` 提供题目读取能力；
- `catalog` 提供分类树和目录顺序；
- `reporting` 负责分类统计、去重口径和导出格式；
- 为兼容前端，原有 `/api/exam/category-stats` 和 `/api/exam/export-category-stats` 路径可以继续由组合路由注册。

P3-A 已将统计聚合、Markdown/Excel 导出和真题 Markdown 导出迁入 `app/modules/reporting/`；`app/modules/exam/router.py` 保留题库接口，组合路由继续注册原有 `/api/exam` 路径。reporting 通过 `ExamQueryService` 和 `CatalogReadService` 的只读 DTO 获取题目与目录数据。

统计规则保持现有文档定义：题目 ID 去重数、分类引用数、章节子树汇总数和未归档分类分别表达。

### 7.2 图片资源

将当前 `UploadService` 拆为三个职责：

1. `media/service.py`：上传、删除、清理等业务用例；
2. `integrations/local_file_storage.py`：安全路径、临时文件、原子替换和文件读写；
3. `media/reference_reader.py`：读取真题和模拟题文本并判断引用关系。

P3-B 已将上传路由、媒体 Service、图片引用读取和本地文件存储迁入 `app/modules/media/` 与 `app/integrations/local_file_storage.py`；media 通过 `ExamQueryService`、`MockQueryService` 的只读 DTO 扫描引用，不直接访问题库 Repository。媒体模块不得直接修改题目内容，也不得把图片删除结果当成题目更新操作。

## 8. 数据库迁移策略

### 8.1 第一阶段：只重组代码

- 保留现有表名、字段名、外键和唯一约束。
- 保留题目选项和分类 JSON 存储格式。
- 只拆分 Model 文件和 Repository 所属位置。
- 不删除、覆盖或重建现有数据库。
- 不创建数据库迁移链。

### 8.2 第二阶段：可选的数据模型改造

长期建议将分类名称 JSON 改为关联表：

```text
exam_question_category(question_id, category_id)
mock_question_category(mock_id, category_id)
```

收益：

- 分类改名不会产生字符串孤立引用；
- 可以使用外键约束；
- 分类统计可直接使用 JOIN 和聚合；
- 不再依赖 JSON 字符串 `LIKE` 查询。

该改造会改变数据库结构，必须单独编写数据方案。实际删除或重建数据库前，需要明确数据库绝对路径、数据损失范围、重建方式和检查方式，并获得用户授权。

## 9. 分阶段执行计划

| 阶段 | 目标 | 主要范围 | 前置条件 | 优先级/规模 | 验收与回滚 |
|---|---|---|---|---|---|
| P0 | 冻结契约和遗留边界 | API、表结构、分类体系、无引用模型清单 | 本文档确认 | P0 / S | 形成迁移前清单；不改代码 |
| P1 | 建立模块骨架并迁移基础模块 | `auth`、`catalog`、路由聚合、依赖归位 | P0 完成 | P1 / M | 已完成文件归位和导入更新；启动入口和 API 路径不变；应用导入核对已在 P2-A 执行记录中覆盖 |
| P2 | 迁移题库核心 | `question_content`、`exam`、`mock`；分为 P2-A 业务代码迁移和 P2-B ORM 模型拆分 | P1 完成且本节 P2 设计确认 | P1 / L | P2-A、P2-B 均已完成结构/导入核对；表名、字段、约束和数据库结构不变 |
| P3 | 迁移统计与媒体 | 分为 P3-A `reporting` 和 P3-B `media`/本地文件存储 | P2 完成 | P1 / L | P3-A、P3-B 均已完成；统计口径、导出和图片引用结果保持不变 |
| P4 | 清理和必要修复 | 遗留模型代码、必需导入修复、循环依赖及列表 N+1 修复 | P1-P3 完成 | P2 / M | 静态清理、遗留模型代码清理、边界复核、只读回归和列表 N+1 优化已完成；写入/上传/清理回归按范围外处理 |
| P5 | 可选 `src` 迁移 | `app` → `src/web408`、启动脚本和构建配置 | P4 稳定且明确需要 | P2 / M | 单独验证安装、启动和导入；失败可整体回退 |

### 9.1 提交和回滚规则

- 每个业务模块独立提交，避免一次提交同时移动所有目录。
- 先移动代码并保持旧 API，再删除旧门面和旧目录。
- 仓库内所有导入在对应阶段一次性更新，不保留旧 `app.services`、`app.repositories` 等内部兼容别名。
- 第一阶段只修复阻塞迁移或确定性运行错误，例如缺失的 `json` 导入；N+1 和其他性能优化另列后续任务。
- 第一阶段不修改数据库结构，代码迁移失败时直接回退对应提交。
- 数据库结构改造与代码模块迁移不得放在同一阶段执行。

### 9.2 P2 设计方案（文档先行）

P2 只进行题库核心代码的内部结构迁移，不改变公开 API、数据库结构或现有题目数据语义。P2 分为两个可独立回滚的子阶段：先迁移业务代码，再拆分 ORM 模型文件。

#### 9.2.1 P2 范围与冻结事项

- 保留当前实际的 `/api/exam/...`、`/api/mock/...` 路径、POST 方法、请求字段、响应信封和错误语义。
- 模块开发设计文档中 `query-page`、`query-detail` 等目标路径仅作未来设计示例，P2 不直接替换现有路径。
- 保留 `ExamQuestion` 与 `MockQuestion` 两张表及其独立业务语义。
- 保留题目选项和分类的 JSON 文本存储、可空字段、历史分类兼容规则及现有唯一约束。
- 不删除、覆盖、清理或重建 `backend-fastapi/data/web408.db`。
- 不在 P2 同步开展 N+1 查询、统计性能、分类 JSON 规范化或 API 路径优化。
- 当前学习页使用持久化 `ExamCategory` 树、管理页使用模拟题 JSON 动态分类的差异暂不修复；统一模拟题分类树另立功能任务。

#### 9.2.2 P2-A：题库业务代码迁移

| 当前代码 | P2 目标位置 | 处理方式 |
|---|---|---|
| `app/api/exam.py` | `app/modules/exam/router.py` | 保留全部现有真题接口路径，移除对 `ExamService` 的依赖 |
| `app/api/mock.py` | `app/modules/mock/router.py` | 保留全部现有模拟题接口路径，移除对 `MockService` 的依赖 |
| `app/schemas/exam.py` | `app/modules/exam/schemas.py` | 当前真题请求、响应及统计/导出类型整体迁移；P3 再拆出 reporting 类型 |
| `app/schemas/mock.py` | `app/modules/mock/schemas.py` | 迁移模拟题请求、响应和统计类型 |
| `app/schemas/question.py` | `app/modules/question_content/schemas.py` | 迁移题目共用字段、选项结构和纯数据校验 |
| `app/repositories/exam_repository.py` | `app/modules/exam/repository.py` | Repository 只负责真题持久化查询，不提交事务 |
| `app/repositories/mock_repository.py` | `app/modules/mock/repository.py` | Repository 只负责模拟题持久化查询，不提交事务 |
| `app/services/exam_query_service.py` | `app/modules/exam/query_service.py` | 迁移查询、分页、索引、详情和响应编排 |
| `app/services/exam_command_service.py` | `app/modules/exam/command_service.py` | 迁移真题写入用例和事务边界 |
| `app/services/mock_query_service.py` | `app/modules/mock/query_service.py` | 迁移查询、来源统计、分类统计和详情 |
| `app/services/mock_command_service.py` | `app/modules/mock/command_service.py` | 迁移模拟题写入用例和事务边界 |
| `app/services/question_mapping.py` | `app/modules/question_content/serialization.py` | 迁移选项/分类 JSON 转换 |
| `app/services/question_validation.py` | `app/modules/question_content/validation.py` 与目录读取边界 | 将纯题型校验和科目/分类校验拆开 |
| `app/services/exam_service.py`、`mock_service.py` | 无 | 删除纯转发门面，不保留旧 Python 导入别名 |

#### 9.2.3 `question_content` 边界

`question_content` 只负责纯内容规则，不导入 `AsyncSession`、Repository、ORM Model 或 HTTP 依赖：

- `schemas.py`：`QuestionOptions`、`QuestionCreateFields`、`QuestionUpdateFields`；
- `serialization.py`：选项和分类 JSON 的序列化、反序列化及无效数据降级；
- `validation.py`：`CHOICE`/`ESSAY` 与选项之间的结构校验。

科目存在性、分类归属和历史分类保留规则属于 `catalog` 的公开读取能力，不放入内容模块。

#### 9.2.4 目录模块的公开读取边界

新增 `app/modules/catalog/read_service.py`，向题库模块提供最小只读边界。该服务返回不可变的只读 DTO，不返回目录 ORM 实体或 HTTP 响应模型，至少覆盖：

- 科目存在性和科目名称读取；
- 指定分类及启用子孙分类名称读取；
- 题目科目/分类归属校验；
- 真题分类统计所需的科目和分类只读数据。

`app/modules/catalog/question_scope_repository.py` 承接原 `QuestionScopeRepository` 的能力并由该边界封装，题库模块不再直接引用 `CategoryRepository` 或 `QuestionScopeRepository`。`category_query_service.py`、题库查询服务和分类统计服务统一从 `question_content` 引入分类解析函数。

P2 暂不重做题目响应中的作者/科目逐条补充查询；Mapper 保持纯转换，查询 Service 继续提供所需显示字段。N+1 优化另列任务。

#### 9.2.5 真题和模拟题模块职责

- `router.py`：依赖注入、权限依赖、响应模型和路径注册；不直接查询数据库。
- `query_service.py`：查询参数编排、分页、统计调用和响应转换；不提交事务。
- `command_service.py`：新增、更新、删除、查重、目录校验和显式事务提交。
- `repository.py`：构造和执行 SQL 查询，不调用 Service，不提交或回滚。
- `mapper.py`：将已取得的实体和显示字段转换为 API DTO，不执行 I/O。

路由通过模块内用例 Service 组合查询、写入和导出能力，不重新创建 `ExamService`、`MockService` 等万能门面。

#### 9.2.6 统计与导出的 P2 过渡边界（已完成）

P2 期间 `reporting` 暂留在顶层 Service，统计与导出通过明确过渡依赖保持原有行为。P3-A 已完成 reporting 迁移：

- `app/modules/reporting/router.py` 注册原有统计与导出相对路径；
- `app/modules/reporting/service.py` 通过 `ExamQueryService`、`CatalogReadService` 的公开只读 DTO 编排业务；
- `app/modules/reporting/exam_category_stats.py` 保留分类聚合规则；
- `app/modules/reporting/exporters.py` 负责 Markdown、Excel 和真题 Markdown 格式生成；
- 原 `app/services/exam_category_stats.py`、`exam_export_service.py` 已删除。

过渡不改变统计口径、导出格式、管理员权限和现有文件下载行为。

#### 9.2.7 P2-B：ORM 模型文件拆分

P2-A 业务代码稳定后，已按本节方案独立拆分原 `app/models/entities.py`：

```text
app/modules/auth/models.py       # User
app/modules/catalog/models.py   # Subject、Chapter、ExamCategory
app/modules/exam/models.py      # ExamQuestion
app/modules/mock/models.py      # MockQuestion
```

`app/models/base.py` 和 `app/models/enums.py` 保留为共享基础模块。模型拆分必须：

- 保持原表名、字段名、外键、索引、唯一约束和可空性；
- 通过字符串关系声明和组合根显式导入处理跨文件关系；
- 在 `main.py` 启动阶段显式加载所有业务模块模型；
- 完成仓库内导入更新后删除 `entities.py`，不保留旧内部兼容别名；
- 不执行数据库迁移、重建或数据清理。

#### 9.2.8 P2 验收与回滚

P2 完成必须同时满足：

1. 现有 `/api/exam`、`/api/mock` 路由清单、POST 方法和响应结构不变；
2. `ExamService`、`MockService` 及题库旧 Repository/Schema/Service 路径不再作为业务入口；
3. `question_content` 无数据库和 HTTP 依赖；
4. 题库模块不直接引用目录私有 Repository；
5. 统计与导出接口仍可用，P3 过渡依赖有明确清单；
6. 题型校验、查重、历史分类、空科目和无效 JSON 处理行为不变；
7. 数据库文件和数据库结构没有变化；
8. 模型显式加载、模块导入和循环依赖完成核对。

P2-A、P2-B 分别独立提交和回滚。若模型拆分出现关系解析或循环导入问题，只回退 P2-B，不回退已确认的业务代码迁移。

#### 9.2.9 P2-A 执行记录

P2-A 已于 2026-09-11 完成代码迁移，实际结果如下：

- 新增 `modules/question_content/`、`modules/exam/`、`modules/mock/` 的 Schema、序列化、校验、Repository、Query Service、Command Service、Mapper 和 Router。
- 新增 `catalog/read_service.py` 及目录只读 DTO；题库不再直接引用目录私有 Repository。
- 删除旧题库 API、Schema、Repository、Query/Command Service 及 `ExamService`、`MockService` 门面；仓库内不保留旧 Python 导入别名。
- `reporting` 和 `media` 暂未迁移，统计导出通过明确的 P2 过渡依赖继续工作。
- 未修改 SQLite 数据库、SQLModel 表名/字段/约束和公开 API。

已执行的结构核对：

- `backend-fastapi/app` 下 57 个 Python 文件 AST 解析通过；
- `import app.main` 成功；
- 真题 15 个、模拟题 13 个 POST 路径与迁移前对照一致；
- 题库旧路径和旧导入引用扫描无残留；Repository 未包含事务提交或回滚。

尚未执行接口实际请求、自动化测试和数据库行为回归；P3-P5 仍待后续实施。

#### 9.2.10 P2-B 执行记录

P2-B 已于 2026-09-11 完成 ORM 模型拆分，实际结果如下：

- `User`、`Subject`、`Chapter`、`ExamCategory`、`ExamQuestion`、`MockQuestion` 已分别归入对应业务模块的 `models.py`。
- `main.py` 已显式加载当前模块模型；跨模块关系使用字符串关系声明。
- 已删除 `app/models/entities.py`，仓库内无旧模型导入路径。
- 表名、字段、外键、索引、约束和数据库文件均未修改。

已执行的模型核对：

- 61 个 Python 文件 AST 解析通过；
- `import app.main` 成功；
- SQLAlchemy `configure_mappers()` 成功；
- 原 9 个模型类及其表名、字段定义与拆分前一致；
- 真题 15 个、模拟题 13 个 POST 路径与迁移前对照一致。

#### 9.2.11 P3-A 执行记录

P3-A 已于 2026-09-11 完成 reporting 迁移，实际结果如下：

- 新增 `app/modules/reporting/` 的 `router.py`、`schemas.py`、`service.py`、`exam_category_stats.py` 和 `exporters.py`。
- 统计与导出路由从 `modules/exam/router.py` 拆出，由组合路由以 `/exam` 前缀继续注册。
- reporting 通过 `ExamQueryService` 和 `CatalogReadService` 的公开只读 DTO 读取题库与目录数据，不直接引用题库 Repository。
- 删除旧 `app/services/exam_category_stats.py` 和 `app/services/exam_export_service.py`。
- 保持原有统计口径、导出格式、管理员权限和 API 路径；未修改数据库。

已执行 `import app.main`、SQLAlchemy Mapper 配置、AST 解析和 15 个 reporting/exam POST 路径对照；接口实际行为和自动化测试仍待执行。

#### 9.2.12 P3-B 执行记录

P3-B 已于 2026-09-11 完成 media 迁移，实际结果如下：

- 新增 `app/modules/media/` 的 `router.py`、`schemas.py`、`service.py` 和 `reference_reader.py`。
- 新增 `app/integrations/local_file_storage.py`，负责临时文件、原子替换、安全路径和本地文件操作。
- media 通过 `ExamQueryService`、`MockQueryService` 的只读 DTO 扫描题目图片引用，不直接引用题库 Repository。
- 删除旧上传路由、上传 Service、图片 Schema 和图片引用 Repository。
- 保持 `/api/upload`、`/uploads/images/{filename}`、管理员权限、清理确认和图片引用行为不变；未修改数据库。

已执行 `import app.main`、SQLAlchemy Mapper 配置、AST 解析和 4 个上传 POST 路径对照；接口实际行为和自动化测试仍待执行。

#### 9.2.13 P4-A 执行记录

P4-A 已于 2026-09-11 完成目录门面和旧边界清理：

- 删除 `app/modules/catalog/category_service.py`，目录路由直接组合 `CategoryQueryService` 与 `CategoryCommandService`。
- 顶层 `app/services/`、`app/repositories/` 目录已移除；旧题库、媒体和模型导入扫描无残留。
- 已核对 64 个 Python 文件的 AST、模块依赖和 import cycle，未发现循环依赖。
- `ResourceFile`、`ExamRandomStat`、`KnowledgePoint` 的 Python 模型及 ORM 关系已移除；对应 SQLite 表和数据未删除。

P4 的遗留模型代码清理和列表 N+1 优化已完成。

#### 9.2.14 P4-B 复核记录

P4-B 已完成静态边界复核：

- 已移除无业务依赖的遗留模型、跨实体关系和 `main.py` 模型注册；对应数据库表仅作为未映射历史表保留。
- `app.services`、`app.repositories`、`app.models.entities`、旧上传和旧图片 Schema 导入均无残留。
- 全部认证、目录、题库、统计和媒体 POST 路径与迁移前保持一致。
- 确认真题/模拟题响应转换仍存在逐题补充科目和作者的 N+1 风险；按迁移范围暂不修改，另列性能任务。

实际写入、上传和清理接口回归未执行。

#### 9.2.15 P4-C 静态接口回归记录

P4-C 已完成静态接口契约核对：

- OpenAPI 包含 61 个 `/api` 路径，所有操作均为 POST。
- 真题、模拟题、上传、目录和 reporting 路径均与迁移前对照一致。
- 所有 `/api` 操作均声明响应结构；OpenAPI 路径数量为 63。
- 未启动服务、未访问 SQLite、未执行实际 HTTP 请求。

#### 9.2.16 P4-D 只读接口回归记录

P4-D 已按授权启动临时后端并执行只读/鉴权边界回归：

- 科目查询、真题分页/年份统计/分类统计、模拟题分页/来源统计、目录树和模拟题分类统计均返回 200。
- 真题 Markdown 导出返回 200 和正确的 UTF-8 `Content-Disposition` 文件名。
- 未认证访问图片列表和管理员分类统计导出返回 401。
- 修复了回归发现的三个确定性问题：题库 Query Service 缺失 `PageInfo` 导入、reporting 路由被题库动态路径遮蔽、导出文件名未进行 Latin-1 安全编码。
- 回归前后 SQLite 数据库文件哈希一致；未调用新增、更新、删除、上传或清理接口。

#### 9.2.17 P4-E 写入边界静态审查记录

P4-E 已完成写入和危险操作的静态审查：

- 真题、模拟题和分类写入 Service 持有事务提交；Repository 不提交或回滚。
- 写入、删除、上传、清理和管理员查重路由均包含 `get_current_admin` 依赖。
- 分类写入使用结构锁；媒体上传使用临时文件和原子替换，清理操作逐文件复核引用。
- media 不直接引用题库 Repository，历史遗留表无业务写入入口。

未执行写入、上传或清理接口；N+1 优化已作为独立 P4-F 任务完成。

#### 9.2.18 P4-F N+1 优化记录

P4-F 已完成真题和模拟题列表响应的 N+1 优化：

- 新增 `AuthReadService`，批量读取作者显示名称。
- `CatalogReadService` 新增批量读取科目名称能力。
- 真题和模拟题 Query Service 的列表响应统一批量读取科目、作者，再执行纯 Mapper 转换。
- 题库 Repository 不再直接读取用户模型；响应字段和查询条件保持不变。
- 页面大小为 10 时，真题和模拟题分页接口当前均使用 4 条 SQL（总数、列表、科目批量、作者批量）。

未修改数据库结构；单条详情仍使用批量接口的单元素集合，不引入额外全局缓存。

## 10. 验收标准

### 10.1 结构验收

- 业务代码位于对应模块，不再从全局 `services/`、`repositories/` 查找所有业务。
- Repository 不包含 `commit()`、`rollback()` 或 HTTP 异常。
- Schema 不执行 I/O 或数据库查询。
- 模块之间没有反向私有导入和循环依赖。
- 跨模块读取只使用公开 Service 和只读 DTO。
- 应用启动阶段显式注册全部需要保留的 SQLModel 模型。
- 不存在仅用于目录对称的空文件。

### 10.2 行为验收

- 现有 API URL、POST 方法和响应信封保持不变。
- 登录、管理员权限、分类树、真题、模拟题、统计、导出和图片功能行为保持不变。
- 分类统计继续区分去重题目数和分类引用数。
- 图片仍使用 `/uploads/images/{filename}` 相对路径。
- 题型校验、重复校验和历史分类处理规则不被迁移过程改变。

### 10.3 质量验收

- 已修复 `json` 导入问题；继续修复其他阻塞迁移的确定性错误。
- 大型文件按独立变化原因拆分，不为减少文件数量重新合并。
- 关键查询、写入和异常边界完成人工核对。
- 列表响应 N+1 已在 P4-F 批量读取；统计性能、分类 JSON 规范化和其他性能优化仍单独评估。

### 10.4 当前执行结果

- P1、P2-A、P2-B、P3-A reporting 和 P3-B media 的代码归位、导入边界和 ORM Mapper 配置已完成。
- P4-A～P4-F 的静态清理、边界复核、接口契约核对、只读回归、写入边界审查和列表 N+1 优化已完成。
- 历史遗留表继续保留但不再由 ORM 映射；写入/上传/清理回归按当前范围不执行，统计性能和分类 JSON 规范化仍待独立任务。
- P5 `src` 迁移尚未执行。
- 应用导入、模型配置、静态路由对照和只读接口回归已完成。

## 11. 已确认决策与剩余讨论

### 11.1 首轮迁移已确认

1. 保留 `backend-fastapi/app` 包结构和 `app.main:app` 启动入口，不迁移到 `src`。
2. `ExamCategory` 是题库唯一正式分类体系。
3. 首轮只迁移代码结构，不修改数据库结构、不重建数据库。
4. 现有 API 路径、POST 方法、请求响应字段和 `{code,message,data}` 契约全部保持不变。
5. `Chapter` 接口和模型继续保留在 `catalog`，与题库分类逻辑隔离。
6. 采用分阶段迁移，每个阶段保持可运行并可独立回滚。
7. 首轮不开展统计性能、分类 JSON 规范化等大范围性能优化；列表响应的确定性 N+1 问题已在 P4-F 单独修复。
8. 不保留旧 Python 内部导入路径的过渡兼容层，仓库内导入统一更新。
9. 未形成业务闭环的旧模型和章节能力保留并隔离，不在首轮删除源码对应的数据表。
10. 统计、媒体和题库之间不直接引用对方 Repository，统一使用公开 Service 和只读 DTO。
11. `ExamQuestion` 与 `MockQuestion` 保持独立实体，只共享题目内容规则。
12. SQLModel 模型由应用组合根显式加载当前业务模块模型；历史遗留表不再建立 ORM 映射。
13. 保留当前 `backend-fastapi/data/web408.db` 及其现有数据差异，不执行数据库覆盖、回滚或清理。
14. P2 先执行 P2-A 题库业务代码迁移，再执行 P2-B ORM 模型文件拆分；两个子阶段独立提交和回滚。
15. P2 保持当前真实 API 路径、POST 方法、请求响应字段和错误语义；模块文档中的 `query-*` 路径不在 P2 采用。
16. `question_content` 只负责纯内容 Schema、序列化和题型校验；科目/分类校验通过 `catalog` 公开只读 Service 完成。
17. P2 暂不迁移 `reporting`；统计与导出接口保留现有行为，并允许使用明确标记的 P2 过渡依赖，P3 完成后清理。
18. 当前模拟题持久化分类树与 JSON 动态分类的差异不在 P2 修复；该事项作为独立功能变更处理。
19. P2-A、P2-B、P3-A reporting、P3-B media 和 P4 静态清理/审查已按本文件分阶段执行并完成；写入回归和 N+1 优化不纳入本轮。

### 11.2 后续仍需讨论但不阻塞首轮归位

1. `Chapter` 接口和模型继续保留；`resource_file`、`exam_random_stat`、`knowledge_point` 历史表暂不删除，避免破坏现有数据库数据。
2. 是否在未来允许将题目分类 JSON 改为关联表；该事项不进入首轮代码迁移。
3. `subject_id` 是否继续允许为空；首轮按当前行为保留。
4. 首轮迁移完成后是否单独进行 `src/web408` 包布局迁移；不与本次模块归位合并。
5. N+1 查询、统计性能和路径配置优化作为后续独立任务，不与首轮结构迁移混合。

当前首轮基线为：**P1、P2-A、P2-B、P3-A reporting、P3-B media 和 P4 静态清理/审查及列表 N+1 优化已完成，保留 `app` 入口、以 `ExamCategory` 为唯一分类、只迁代码并修复必需错误、完全保持 API 兼容、不保留旧内部导入别名；P5 待后续评估**。
